const { HTTP_STATUS, HTTP_STATUS_TEXT } = require("./httpStatus");
const {
  MESSAGES,
  AUTH_MESSAGES,
  USER_MESSAGES,
  ERROR_MESSAGES,
} = require("./messages");

module.exports = {
  HTTP_STATUS,
  HTTP_STATUS_TEXT,
  MESSAGES,
  AUTH_MESSAGES,
  USER_MESSAGES,
  ERROR_MESSAGES,
};

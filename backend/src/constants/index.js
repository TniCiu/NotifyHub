const { HTTP_STATUS, HTTP_STATUS_TEXT } = require("./http.constant");
const { MESSAGES } = require("./message.constant");
const { ERROR_CODE } = require("./errorCode.constant");
const {
  NOTIFICATION_CHANNEL,
  NOTIFICATION_CHANNELS,
  NOTIFICATION_STATUS,
  NOTIFICATION_STATUSES,
} = require("./notification.constant");

module.exports = {
  HTTP_STATUS,
  HTTP_STATUS_TEXT,
  MESSAGES,
  ERROR_CODE,
  NOTIFICATION_CHANNEL,
  NOTIFICATION_CHANNELS,
  NOTIFICATION_STATUS,
  NOTIFICATION_STATUSES,
};
